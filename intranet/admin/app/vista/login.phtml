<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Geofal - Admin</title>
    
    <!-- Favicon Geofal SVG -->
    <link rel="icon" type="image/svg+xml" href="<?php echo ruta ?>assets/Logo_Geofal_favicon.svg">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: #ffffff;
            min-height: 100vh;
            color: #333333;
            overflow-x: hidden;
        }

        /* Logo en esquina superior izquierda */
        .login-logo-header {
            position: fixed;
            top: 40px;
            left: 40px;
            z-index: 9999;
            padding: 0;
            margin: 0;
            background: transparent;
            width: auto;
            height: auto;
        }

        .login-logo-img {
            max-width: 280px;
            min-width: 150px;
            width: 100%;
            height: auto;
            display: block !important;
            visibility: visible !important;
            opacity: 1 !important;
            object-fit: contain;
            background: transparent;
            border: none;
            margin: 0;
            padding: 0;
        }

        /* Sección principal - Formulario centrado */
        .login-main-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 100px 20px 40px;
            background: #ffffff;
            position: relative;
            margin-top: 0;
        }

        /* Formulario */
        .login-form-container {
            width: 100%;
            max-width: 400px;
        }

        .login-title {
            font-size: 28px;
            font-weight: 600;
            color: #333333;
            text-align: center;
            margin-bottom: 30px;
        }

        .login-form {
            width: 100%;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            font-size: 14px;
            font-weight: 500;
            color: #333333;
            margin-bottom: 8px;
        }

        .form-control {
            width: 100%;
            padding: 12px 16px;
            font-size: 16px;
            border: 1px solid #B0D4F1;
            border-radius: 6px;
            background: #E8F0F8;
            color: #333333;
            font-family: 'Roboto', sans-serif;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            outline: none;
            border-color: #1A4D5E;
            background: #ffffff;
            box-shadow: 0 0 0 3px rgba(26, 77, 94, 0.1);
        }

        .form-control::placeholder {
            color: #999999;
        }

        .btn-submit {
            width: 100%;
            padding: 14px 20px;
            font-size: 16px;
            font-weight: 600;
            text-transform: uppercase;
            color: #ffffff;
            background: #1A4D5E;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Roboto', sans-serif;
            letter-spacing: 0.5px;
        }

        .btn-submit:hover {
            background: #153d4d;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(26, 77, 94, 0.3);
        }

        .btn-submit:active {
            transform: translateY(0);
        }

        .alert {
            padding: 12px 16px;
            border-radius: 6px;
            margin-bottom: 20px;
            font-size: 14px;
        }

        .alert-danger {
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
        }

        /* Sección inferior - Footer visible con scroll */
        .login-hero-footer {
            background: transparent;
            color: #FFFFFF;
            padding: 0;
            display: flex;
            align-items: stretch;
            justify-content: flex-end;
            position: relative;
            overflow: hidden;
            min-height: 300px;
            width: 100%;
        }

        .hero-image {
            width: 100%;
            min-height: 300px;
            background-image: url('<?php echo ruta ?>publico/img/footer.jpeg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }

        .hero-footer-links {
            position: absolute;
            bottom: 20px;
            right: 20px;
            font-size: 12px;
            color: rgba(255, 255, 255, 0.8);
            z-index: 10;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .login-logo-header {
                top: 20px;
                left: 20px;
                position: fixed;
            }

            .login-logo-img {
                max-width: 200px;
                min-width: 120px;
            }

            .login-main-section {
                padding: 80px 20px 40px;
            }

            .login-title {
                font-size: 24px;
            }

            .login-hero-footer {
                min-height: 250px;
            }

            .hero-image {
                width: 100%;
                min-height: 250px;
            }

            .hero-footer-links {
                bottom: 15px;
                right: 15px;
                font-size: 11px;
            }
        }
    </style>
</head>
<body>
    <!-- Logo en esquina superior izquierda -->
    <div class="login-logo-header">
        <?php 
        $logo_filename = 'Logo Geofal (1).svg';
        $logo_path = ruta . 'assets/' . rawurlencode($logo_filename);
        ?>
        <img 
            src="<?php echo htmlspecialchars($logo_path); ?>" 
            alt="Geofal - Ingeniería y laboratorio de materiales" 
            class="login-logo-img"
        >
    </div>

    <!-- Sección principal - Formulario centrado -->
    <div class="login-main-section">
        <div class="login-form-container">
            <h1 class="login-title">Iniciar Sesión</h1>
            
            <form action="<?php echo ruta ?>login" method="post" class="login-form">
                <?php 
                $error = isset($_GET['error']) ? $_GET['error'] : '0';
                $b_get = isset($_GET['b']) ? $_GET['b'] : '0';
                
                if ($error == '1' || $error == 'error'): ?>
                    <div class="alert alert-danger" role="alert">
                        Tu contraseña o usuario está mal escrito, inténtalo nuevamente
                    </div>
                <?php endif; ?>

                <!-- Campo Usuario -->
                <div class="form-group">
                    <label for="dni" class="form-label">Usuario</label>
                    <input 
                        type="text" 
                        id="dni" 
                        name="dni" 
                        class="form-control" 
                        autocomplete="username"
                        required
                        aria-required="true"
                    >
                </div>

                <!-- Campo Contraseña -->
                <div class="form-group">
                    <label for="clave" class="form-label">Contraseña</label>
                    <input 
                        type="password" 
                        id="clave" 
                        name="clave" 
                        class="form-control" 
                        autocomplete="current-password"
                        required
                        aria-required="true"
                    >
                </div>

                <!-- Campos ocultos -->
                <input type="hidden" name="login" value="ok">
                <input type="hidden" name="r" value="<?php echo htmlspecialchars($b_get); ?>">

                <!-- Botón de envío -->
                <button type="submit" class="btn-submit">
                    INGRESAR
                </button>
            </form>
        </div>
    </div>

    <!-- Sección Inferior: SOLO VISIBLE CON SCROLL -->
    <div class="login-hero-footer">
        <div class="hero-image" role="img" aria-label="Trabajadores en campo con equipo de seguridad"></div>
        <div class="hero-footer-links">
            Privacidad - Condiciones
        </div>
    </div>
</body>
</html>
